CREATE TABLE picture_metadata
(
    id             text,
    author         text,
    camera         text,
    tags           text,
    cropped_picture text,
    full_picture    text,
    search_terms    text,
    PRIMARY KEY (id)
)